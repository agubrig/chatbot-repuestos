const express = require('express'); const axios = require('axios'); const twilio = require('twilio'); const app = express(); app.use(express.json()); app.use(express.urlencoded({extended: false})); const client = twilio(process.env.TWILIO_SID, process.env.TWILIO_TOKEN); app.post('/webhook', async (req, res) => { res.set('Content-Type', 'text/xml'); res.send('<Response></Response>'); const from = req.body.From; const text = req.body.Body; if (!from || !text) return; try { const r = await axios.post(process.env.FLOWISE_URL, {question: text, overrideConfig: {sessionId: from}}); await client.messages.create({from: 'whatsapp:+14155238886', to: from, body: r.data.text}); } catch(e) { console.error(e.message); } }); app.listen(process.env.PORT || 3001, () => console.log('Bot corriendo'));
